- name : installation package
  hosts : all
  become : true
  tasks :
    - name : install httpd
      package :
        name: httpd
        state: present
        ignore_errors: True  
 
    - name : install apache2
      package :
        name: apache2
        state: present