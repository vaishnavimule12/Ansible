- name : install packages
  hosts : all
  become : True
  tasks :
    - name : update apt
      shell :
        cmd : apt-get update
    - name : install apache2
      apt:
        name: apache2
        state: present
    - name : install httpd
      yum:
        name: httpd
        state: present
      ignore_errors: True